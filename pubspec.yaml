name: app_movil_estilo_dorado
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # Dependencias recomendadas para Estilo Dorado
  dio: ^5.7.0
  provider: ^6.1.2
  go_router: ^14.0.0
  flutter_secure_storage: ^9.2.2
  cached_network_image: ^3.4.1
  intl: ^0.19.0
  http: ^1.2.2   # opcional, dio es mejor
  # webview_flutter: ^4.14.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/banners/
    - assets/icons/